import styled from "styled-components";

const Background = styled("div")`
  background: linear-gradient(30deg, rgba(0, 0, 0, 1), rgba(0, 0, 0, 0.8));
  min-height: calc(100vh - 100px);
`;

export default Background;
